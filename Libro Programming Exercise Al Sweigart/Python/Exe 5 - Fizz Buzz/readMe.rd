Programa en Pygame que simula el juego Fizz Buzz. En el cuál se sustituyen números 
naturales en una rejilla de la siguiente manera:

1. Si el número es divisible por 3 se le remplaza por la letra F (Fizz).
2. Si el número es divisible por % se le remplaza por la letra B (Buzz).
3. Si el número es divisible por 3 y 5 se le remplaza por las letras FB (FizzBuzz).

Este progra utiliza una clase en Python llamada Cell. Que genera rectángulos en
pantalla con un texto dentro (número o letra) y posee la lógica del juego FizzBuzz.